import React from "react";
import Form from "../Form/Form"

function Contact(props) {
    return (
        <div>
            <h1>Contact Page</h1>
            <Form />
        </div>
    );
}

export default Contact;
